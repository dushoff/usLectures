
For this assignment, you may use AI, or get help from classmates or the teaching team. Report if you used such assistance.

Do _not_ report results that you didn't run. If necessary, run your code on a classmate's machine for Q1, and just tell us where you got stuck on Q2. Best if you can finish during your tutorial and not have to worry about this.

1. Install R and rstudio, [using the instructions here](https://nathanieldphillips-yarrr.share.connect.posit.cloud/installing-base-r-and-rstudio.html) (or other instructions, if you prefer).

	a) Find the "console" window in rstudio and enter the following code, one line at a time (use your actual student number, which is a number, and no angle brackets):

		```
		set.seed(<yourStudentNumber>)
		runif(2)
		```

	b) Report the result (the response from R). How do you interpret this? It's fine to guess, or to google, or to ask an agent...

2. Find the "packages" window and use "install" to install a package called "tidyverse". Now type `library(dplyr)` in the console window and report what you get. Don't bother trying to explain that.
